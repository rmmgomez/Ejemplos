// console.log('pisha prueba');

/* 
export const sumar = (num1, num2) => num1 + num2;
export const restar = (num1, num2) => num1 - num2;
*/
const sumar = (num1, num2) => num1 + num2;
const restar = (num1, num2) => num1 - num2;
const pinocho = 3.14;

export {
    sumar, restar, pinocho
};